# Phase 5 Proposal: Boss Battle Loop

Add a configured boss encounter without changing Player, weapons, ordinary
Enemy, or Shop ownership. A configured round spawns one Boss; defeat credits a
current-run reward and completes the round, while timeout follows the existing
failure dialog path.

Success checks: spawn threshold, phase transitions, telegraph response, one-shot
reward, timeout handling, and transition to Shop.
